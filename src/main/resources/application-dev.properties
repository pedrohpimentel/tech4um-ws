# ====================================================================
# PERFIL 'DEV' (Development - Ambiente Local)
# Usado para rodar a aplicação com o MySQL local.
# ====================================================================

# Configurações do DataSource (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/project_ws_db?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=1234567
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configurações do JPA/Hibernate (MySQL)
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
